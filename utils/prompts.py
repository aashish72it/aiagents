
calc_prompt = "Compute the arithmetic expression with safety checks."

sql2dbt_system_prompt = (
    "Convert the provided SQL into a dbt model file. Ensure a proper Jinja config block,"
    " stable naming, and document assumptions."
)

search_prompt = "Search DuckDuckGo for the given query and return top concise results."
